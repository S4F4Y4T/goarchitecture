# ── Auth service ─────────────────────────────────────────────────────────────
AUTH_APP_PORT=6868
AUTH_LOG_LEVEL=info
AUTH_CORS_ALLOWED_ORIGINS=https://yourdomain.com   # never use * in production

# ── JWT ──────────────────────────────────────────────────────────────────────
# Generate keys:
#   openssl genrsa -out deploy/kong/jwt.key 2048
#   openssl rsa -in deploy/kong/jwt.key -pubout -out deploy/kong/jwt.key.pub
JWT_PRIVATE_KEY_PATH=/app/deploy/kong/jwt.key
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=168h
COOKIE_SECURE=true               # must be true in production (requires HTTPS)

# ── User service ────────────────────────────────────────────────────────────
USER_APP_PORT=6969
USER_GRPC_PORT=6970
USER_LOG_LEVEL=info
USER_DB_PORT=5433
USER_DB_USER=postgres
USER_DB_PASSWORD=<change-me>
USER_DB_NAME=user_db
USER_DB_SSLMODE=require          # set to 'disable' only for local dev
USER_DB_MAX_OPEN_CONNS=25
USER_DB_MAX_IDLE_CONNS=25
USER_DB_CONN_MAX_LIFETIME=5m
USER_DB_CONN_MAX_IDLE_TIME=5m
USER_CORS_ALLOWED_ORIGINS=https://yourdomain.com   # never use * in production

# ── Catalog service ──────────────────────────────────────────────────────────
CATALOG_APP_PORT=7070
CATALOG_LOG_LEVEL=info
CATALOG_DB_PORT=5434
CATALOG_DB_USER=postgres
CATALOG_DB_PASSWORD=<change-me>
CATALOG_DB_NAME=catalog_db
CATALOG_DB_SSLMODE=require       # set to 'disable' only for local dev
CATALOG_DB_MAX_OPEN_CONNS=25
CATALOG_DB_MAX_IDLE_CONNS=25
CATALOG_DB_CONN_MAX_LIFETIME=5m
CATALOG_DB_CONN_MAX_IDLE_TIME=5m
CATALOG_CORS_ALLOWED_ORIGINS=https://yourdomain.com   # never use * in production
CATALOG_RATE_LIMIT_REQUESTS=100
CATALOG_RATE_LIMIT_WINDOW=1m

# ── Shared Redis ─────────────────────────────────────────────────────────────
REDIS_PORT=6380
REDIS_PASSWORD=<change-me>
REDIS_DB=0

# ── Kong API Gateway ─────────────────────────────────────────────────────────
KONG_HTTP_PORT=8000
KONG_ADMIN_PORT=8002

# ── PgAdmin ──────────────────────────────────────────────────────────────────
PGADMIN_EMAIL=admin@yourdomain.com
PGADMIN_PASSWORD=<change-me>
PGADMIN_PORT=5050
